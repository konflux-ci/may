apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: driver-aws
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager
  namespace: system
  # IAM web-identity trust must match the rendered ServiceAccount subject
  # (system:serviceaccount:<namespace>:<name>), not necessarily the base
  # namespace/name in this file. See AGENTS.md and your kustomize overlay.
