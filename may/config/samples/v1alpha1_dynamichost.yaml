apiVersion: may.konflux-ci.dev/v1alpha1
kind: DynamicHost
metadata:
  labels:
    app.kubernetes.io/name: may
    app.kubernetes.io/managed-by: kustomize
  name: dynamichost-sample
spec:
  # TODO(user): Add fields here
